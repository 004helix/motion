Builds the latest [**motion**](https://github.com/Motion-Project/motion.git) code from the github repository.

```
docker run -ti --rm -v /path/to/motion.conf:/etc/motion/motion.conf:ro 004helix/motion
```
